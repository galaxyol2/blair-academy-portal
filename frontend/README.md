# Frontend

Static starter UI scaffold to match the general feel of `blair.edu` (clean, editorial typography, strong header/hero).

## Run locally

- Open `frontend/index.html` in your browser, or
- Use any local static server (optional), e.g. `python -m http.server` from the `frontend/` folder.

