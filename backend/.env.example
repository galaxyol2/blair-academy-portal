# Backend environment variables (example)
#
# Copy this file to `backend/.env` locally (do not commit real secrets).

# App
PORT=3001
NODE_ENV=development
JWT_SECRET=change-me-in-dev

# Data file (dev JSON store)
# DATA_FILE=./data/dev.json

# Database (recommended for production)
# Railway Postgres will provide DATABASE_URL automatically when you add it.
# DATABASE_URL=
# PGSSLMODE=disable

# CORS (optional)
# CORS_ORIGIN=http://localhost:3000

# Password reset link base (dev)
# Example: http://localhost:8000/frontend
# FRONTEND_BASE_URL=http://localhost:8000/frontend

# SMTP (optional - to send real reset emails)
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM="Blair Portal <no-reply@example.com>"

# SendGrid API (recommended on Railway if SMTP ports are blocked)
# SENDGRID_API_KEY=
